import { Box, Text } from '@chakra-ui/react';
import React from 'react';

const Focus = () => {
    return (
      <Box>
        <Text fontSize="5xl" color={"green.600"}>
          This is focus
        </Text>
      </Box>
    );
};

export default Focus;